"""Scene identifiers."""

from __future__ import annotations

from enum import Enum


class Scene(Enum):
    FISHING = "fishing"
    MINIGAME = "minigame"
    SHOP = "shop"
    DIARY = "diary"
    COMBAT = "combat"
    VICTORY = "victory"
